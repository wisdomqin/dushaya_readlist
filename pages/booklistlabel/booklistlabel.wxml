<safe-area-top></safe-area-top>
<view class="page-container">
  <scroll-view scroll-y class="scroll-view" style="height: calc(100vh - {{safeAreaTopHeight}}px);">
    <text class="tag-name">{{tag.name}}</text>
    <text class="tag-description">{{tag.description}}</text>
    <view class="book-list">
      <view wx:for="{{bookLists}}" wx:key="_id" class="book-card" bindtap="goToBookList" data-id="{{item._id}}">
        <view class="book-info">
          <text class="book-title">{{item.title}}</text>
          <text class="book-author">{{item.author}}</text>
          <text class="book-description">简介：{{item.description}}</text>
        </view>
        <view class="book-rating">
          <text class="rating-score">{{item.rating || '暂无评分'}}</text>
          <text class="rating-count" wx:if="{{item.ratingCount}}">{{item.ratingCount}}人评分</text>
        </view>
      </view>
    </view>
  </scroll-view>
</view>